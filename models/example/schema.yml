
version: 2

models:
  - name: my_first_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
          
  - name: GMCD_new
    description: "View copy for GMCD_trial"
    columns:
      - name: id
        description: "The primary key for this table"
  
  - name: Commcare_runs
    description: "Copy of commcare export runs"
    columns:
      - name: id
        description: "The primary key for this table"